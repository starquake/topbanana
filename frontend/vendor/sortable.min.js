// Vendored SortableJS entry: sourced from the npm `sortablejs` package (pinned
// in package.json) and re-emitted by esbuild as a standalone classic script at
// the served path the admin quiz-reorder template references
// (/assets/js/vendor/sortable.min.js). It exposes the Sortable constructor on
// window.Sortable, matching the upstream UMD build's global (#199, #897).
import Sortable from 'sortablejs';

window.Sortable = Sortable;
