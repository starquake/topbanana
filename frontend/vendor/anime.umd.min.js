// Vendored anime.js entry: sourced from the npm `animejs` package (pinned in
// package.json) and re-emitted by esbuild as a standalone classic script at the
// served path the templates reference. It exposes the full v4 namespace on
// window.anime (animate, utils, spring, ...) so the shared runAnim wrapper finds
// window.anime.animate, matching the upstream UMD build's single global (#897).
import * as anime from 'animejs';

window.anime = anime;
