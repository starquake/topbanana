// Vendored Alpine.js entry: sourced from the npm `alpinejs` package (pinned in
// package.json) and re-emitted by esbuild as a standalone classic script at the
// served path the templates reference (/assets/js/vendor/alpine.min.js and
// /client/js/vendor/alpine.min.js). It reproduces the upstream cdn build's
// auto-bootstrap: expose window.Alpine for x-data expressions, then start once
// the microtask queue drains so deferred init code can register first (#897).
import Alpine from 'alpinejs';

window.Alpine = Alpine;
queueMicrotask(() => Alpine.start());
