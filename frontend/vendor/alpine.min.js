// Vendored Alpine.js entry: sourced from the npm `alpinejs` package (pinned in
// package.json) and re-emitted by esbuild as a standalone classic script at the
// single served path the templates reference (/static/js/vendor/alpine.min.js);
// the player client loads this same web-served copy. It reproduces the upstream
// cdn build's auto-bootstrap: expose window.Alpine for x-data expressions, then
// start once the microtask queue drains so deferred init code can register
// first (#897).
import Alpine from 'alpinejs';

window.Alpine = Alpine;
queueMicrotask(() => Alpine.start());
