{{/* The editor pane's empty state (#1260). Its own partial so a delete can
     swap it back in after clearing the deleted question or round. */}}
{{define "editor_empty"}}
<p class="text-text-dim text-[0.95rem]">Pick a question or round on the left to edit it.</p>
{{end}}
